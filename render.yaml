services:
  - type: web
    name: mon-proxy-personnel
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: PROXY_USER
        value: "mon_utilisateur"
      - key: PROXY_PASS
        value: "mon_mot_de_passe_securise"
